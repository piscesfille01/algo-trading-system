# Firebase Schema

```
/recommendations/{date}/
  picks, marketSentiment, vix, createdAt

/portfolio/
  balance, initialBalance, positions

/trades/{id}/
  symbol, buyPrice, sellPrice, qty, recommendedAt, closedAt, pnl, pnlPercent, hit

/performance/
  totalTrades, winTrades, lossTrades, hitRate, totalPnl
```
